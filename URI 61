#include <stdio.h>

int main()
{
    char n;
    double sal, v, t;
    
    scanf("%s", &n);
    scanf("%lf %lf", &sal, &v);
    
    t = (sal + (v*0.15));
    
    printf("TOTAL = R$ %.2lf\n", t);
    
return 0;
}
