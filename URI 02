#include <stdio.h>
int main()
{
    int count, j;
    count = 1;
    j = 7;
    
    while (count <= 9) 
    {
        printf("I=%d J=%d\n", count, j);
        j -- ;

        if (j == 4) 
        {
            j = 7;
            count += 2;
        }
    }

return 0;
}
