#include <stdio.h>
 
int main()
{
    double azao, bzao, czao, A, B, C, alto;
    scanf("%lf %lf %lf", &azao, &bzao, &czao);
    scanf("%lf %lf %lf", &A, &B, &C);
 
    alto = (bzao*czao) + (B*C);
    
    printf("VALOR A PAGAR: R$ %.2lf\n", alto);
 
    return 0;
}
